# Sport Izar SMAM — Frontend (React + Vite)

## Quick start
```bash
cd frontend
npm install
npm run dev
```

Frontend runs on `http://localhost:5173`.

> Backend must run on `http://localhost:5000` (CORS already configured).
